spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookStore
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
    

redis.server.host: "172.18.0.2"
redis.server.port: "6379"
cacheService.host: "172.18.0.2"
cacheService.port: "8003"
cache.service.url: "http://localhost:8004"
bookService.host: "172.18.0.2"
bookService.port: "8004"
